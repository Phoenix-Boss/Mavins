apply from: new File(
    ["node", "--print", "require.resolve('expo/package.json')"].execute(null, rootDir).text.trim(),
    "../scripts/autolinking.gradle"
).asFile()
useExpoModules()

// Main app
include ':app'

// Only mavin-eq remains; it now builds the MWEngine native code
include ':mavin-eq'

project(':mavin-eq').projectDir = new File(rootDir, '../modules/mavin-eq/android')
